# FlexEdgeAdmin — Production Docker Compose (overlay)
#
# Usage:
#   docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml up -d --build
#
# This overlays the base docker-compose.yml with:
#   - nginx reverse proxy with TLS (Let's Encrypt)
#   - certbot sidecar for automated certificate renewal
#   - flexedge-web only exposed internally (no direct port mapping)
#
# First-time TLS setup:
#   1. Set DOMAIN and CERTBOT_EMAIL in .env
#   2. Start services:
#      docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml up -d
#   3. Obtain initial certificate:
#      docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml \
#        run --rm certbot certonly --webroot -w /var/www/certbot \
#        -d ${DOMAIN} --email ${CERTBOT_EMAIL} --agree-tos --non-interactive
#   4. Restart nginx to pick up the certificate:
#      docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml restart nginx

services:
  flexedge-web:
    # Override: remove direct port mapping (nginx proxies instead)
    ports: !override
      - "127.0.0.1:8088:8088"

  nginx:
    image: nginx:alpine
    container_name: flexedge-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # Mounted as a TEMPLATE, not a finished config: the nginx image's
      # 20-envsubst-on-templates.sh renders /etc/nginx/templates/*.template
      # into /etc/nginx/conf.d/ at boot, substituting ${DOMAIN}. nginx.conf
      # itself has no shell-style expansion, so a literal ${DOMAIN:-localhost}
      # in the config was a parse error ("closing bracket ... missing").
      - ./nginx.conf:/etc/nginx/templates/default.conf.template:ro
      - certbot-data:/etc/letsencrypt:ro
      - certbot-webroot:/var/www/certbot:ro
    depends_on:
      - flexedge-web
    environment:
      # The :- default is applied HERE by compose (shell syntax is valid in
      # a compose file), so the template only ever sees a plain ${DOMAIN}.
      - DOMAIN=${DOMAIN:-localhost}
      # Restrict envsubst to DOMAIN so nginx's own $host / $request_uri /
      # $remote_addr are never touched during rendering.
      - NGINX_ENVSUBST_FILTER=DOMAIN

  certbot:
    image: certbot/certbot
    container_name: flexedge-certbot
    volumes:
      - certbot-data:/etc/letsencrypt
      - certbot-webroot:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew --quiet; sleep 12h & wait $${!}; done'"

volumes:
  certbot-data:
  certbot-webroot:
